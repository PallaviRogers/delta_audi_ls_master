{
	"name": "trigger_user_rmi",
	"properties": {
		"description": "loads salesforce_mc_nonpi.user_rmi from on-prem hive",
		"annotations": [],
		"runtimeState": "Stopped",
		"pipeline": {
			"pipelineReference": {
				"referenceName": "pl_onprem_adls_delta_audi",
				"type": "PipelineReference"
			},
			"parameters": {
				"windowStart": "@{trigger().outputs.windowStartTime}",
				"windowEnd": "@{trigger().outputs.windowEndTime}",
				"schemaname": "mbuadl_agg_nonpi",
				"tablename": "user_rmi"
			}
		},
		"type": "TumblingWindowTrigger",
		"typeProperties": {
			"frequency": "Hour",
			"interval": 24,
			"startTime": "2020-06-01T16:00:00Z",
			"delay": "00:00:00",
			"maxConcurrency": 50,
			"retryPolicy": {
				"intervalInSeconds": 30
			},
			"dependsOn": []
		}
	}
}